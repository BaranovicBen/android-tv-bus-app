{
  "name": "idsbk-kiosk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "server": "npm --prefix server run dev",
    "web": "npm --prefix web run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}